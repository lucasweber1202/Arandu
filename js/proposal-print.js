(function(){
  const sheet=document.querySelector('[data-proposal-sheet]'); if(!sheet)return;
  const client=document.querySelector('[data-proposal-client]'); const space=document.querySelector('[data-proposal-space]');
  function esc(v){return String(v??'').replace(/[&<>'"]/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
  function items(){try{return JSON.parse(localStorage.getItem('arandu.selection.v1')||'[]');}catch{return[];}}
  function render(){const list=items(); const total=list.reduce((sum,item)=>sum+(Number(item.price)||0),0); sheet.innerHTML='<p class="eyebrow">Arandu · proposta curatorial</p><h1>Seleção de arte brasileira contemporânea.</h1><p><strong>Cliente:</strong> '+esc(client.value||'Cliente em análise')+'<br><strong>Ambiente:</strong> '+esc(space.value||'Ambiente a definir')+'<br><strong>Data:</strong> '+new Date().toLocaleDateString('pt-BR')+'</p><p>Esta proposta reúne obras salvas em seleção para avaliação curatorial, disponibilidade, documentação, prazo de envio e condições comerciais.</p><div class="admin-proposal-items">'+(list.length?list.map((item,i)=>'<article class="admin-proposal-item"><div class="admin-proposal-thumb '+esc(item.thumb||'')+'"></div><div><p class="eyebrow">Obra '+(i+1)+'</p><h2>'+esc(item.title)+'</h2><p>'+esc(item.artist)+'</p><p>'+esc([item.technique,item.dimensions,item.priceLabel].filter(Boolean).join(' · '))+'</p><p>'+esc(item.context||item.note||'Seleção curatorial Arandu.')+'</p></div></article>').join(''):'<div class="op-empty"><strong>Seleção vazia</strong><span>Salve obras antes de gerar a proposta.</span></div>')+'</div><section class="certificate-preview"><p class="eyebrow">Resumo comercial</p><h2>'+esc(list.length)+' obras selecionadas</h2><p>'+esc(total?total.toLocaleString('pt-BR',{style:'currency',currency:'BRL'}):'Valores sob consulta ou em validação.')+'</p><p>A proposta não confirma reserva. A Arandu verifica disponibilidade, certificado, envio e condição antes de qualquer fechamento.</p></section>' ;}
  client?.addEventListener('input',render); space?.addEventListener('input',render); document.querySelector('[data-print-proposal]')?.addEventListener('click',()=>window.print()); render();
})();
