const PANEL_ADMIN_TOKEN_KEY = 'arandu.admin.token';

const PANEL_CONFIG = {
  obras: { endpoint: '/api/admin/artworks', listKey: 'artworks', title: 'Obras cadastradas', empty: 'Nenhuma obra cadastrada ainda.', statusOptions: ['available', 'in_conversation', 'temporarily_reserved', 'sold', 'on_hold', 'not_published'], fields: [['title', 'Obra'], ['artists.name', 'Artista'], ['language', 'Linguagem'], ['status', 'Status'], ['price_cents', 'Preço']] },
  artistas: { endpoint: '/api/admin/artists', listKey: 'artists', title: 'Artistas cadastrados', empty: 'Nenhum artista cadastrado ainda.', statusOptions: ['draft', 'screening', 'approved', 'published', 'paused', 'archived'], fields: [['name', 'Artista'], ['city', 'Cidade'], ['state', 'UF'], ['languages', 'Linguagens'], ['status', 'Status']] },
  certificados: { endpoint: '/api/admin/certificates', listKey: 'certificates', title: 'Certificados emitidos', empty: 'Nenhum certificado emitido ainda.', statusOptions: ['draft', 'valid', 'revoked', 'under_review'], fields: [['code', 'Código'], ['artworks.title', 'Obra'], ['issued_to', 'Emitido para'], ['verification_status', 'Status'], ['issued_at', 'Emissão']] },
  leads: { endpoint: '/api/admin/crm?type=leads', listKey: 'items', title: 'Leads e contatos', empty: 'Nenhum lead recebido ainda.', statusOptions: ['new', 'qualified', 'in_conversation', 'proposal_sent', 'converted', 'archived'], fields: [['name', 'Nome'], ['type', 'Tipo'], ['email', 'Email'], ['whatsapp', 'WhatsApp'], ['status', 'Status']] },
  submissions: { endpoint: '/api/admin/crm?type=submissions', listKey: 'items', title: 'Submissões de artistas', empty: 'Nenhuma submissão recebida ainda.', statusOptions: ['received', 'screening', 'curatorial_review', 'approved', 'not_now', 'published', 'archived'], fields: [['artist_name', 'Artista'], ['city', 'Cidade'], ['languages', 'Linguagens'], ['portfolio_url', 'Portfólio'], ['status', 'Status']] },
  briefs: { endpoint: '/api/admin/crm?type=briefs', listKey: 'items', title: 'Briefings empresariais', empty: 'Nenhum briefing recebido ainda.', statusOptions: ['received', 'reviewing', 'proposal_draft', 'proposal_sent', 'won', 'lost', 'archived'], fields: [['company', 'Empresa'], ['name', 'Contato'], ['environment', 'Ambiente'], ['budget', 'Orçamento'], ['status', 'Status']] }
};

function panelToken() { return localStorage.getItem(PANEL_ADMIN_TOKEN_KEY) || ''; }
function setPanelToken(value) { localStorage.setItem(PANEL_ADMIN_TOKEN_KEY, value.trim()); }
function panelStatus(text, isError = false) { document.querySelectorAll('[data-panel-status]').forEach((node) => { node.textContent = text; node.style.color = isError ? '#7b1f17' : '#6f221b'; }); }
function getPath(object, path) { return path.split('.').reduce((acc, key) => acc && acc[key] !== undefined ? acc[key] : null, object); }
function formatValue(value, field) { if (Array.isArray(value)) return value.join(', '); if (field === 'price_cents' && value !== null && value !== undefined) return (Number(value) / 100).toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' }); if (field.endsWith('_at') && value) return new Date(value).toLocaleString('pt-BR'); if (field.includes('url') && value) return `<a href="${value}" target="_blank" rel="noreferrer">Abrir</a>`; return value || '—'; }
async function panelRequest(url, options = {}) { const response = await fetch(url, { ...options, headers: { 'Content-Type': 'application/json', 'x-arandu-admin-token': panelToken(), ...(options.headers || {}) } }); const data = await response.json().catch(() => ({})); if (!response.ok || data.ok === false) throw new Error(data.error || 'Erro no painel.'); return data; }
function currentConfig() { return PANEL_CONFIG[document.body.dataset.operationalPanel]; }
function currentItems() { return window.__ARANDU_PANEL_ITEMS__ || []; }
function itemStatus(item) { return item.status || item.verification_status || 'sem_status'; }
function searchableText(item) { return JSON.stringify(item).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, ''); }
function activeFilters() { return { q: (document.querySelector('[data-panel-filter-q]')?.value || '').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, ''), status: document.querySelector('[data-panel-filter-status]')?.value || 'all' }; }
function filteredItems() { const { q, status } = activeFilters(); return currentItems().filter((item) => (status === 'all' || itemStatus(item) === status) && (!q || searchableText(item).includes(q))); }
function renderMetrics(items) { const target = document.querySelector('[data-panel-metrics]'); if (!target) return; const byStatus = items.reduce((acc, item) => { const status = itemStatus(item); acc[status] = (acc[status] || 0) + 1; return acc; }, {}); target.innerHTML = `<div class="grid grid-4"><article class="card"><h3>${items.length}</h3><p>Total filtrado</p></article>${Object.entries(byStatus).slice(0, 7).map(([status, count]) => `<article class="card"><h3>${count}</h3><p>${status}</p></article>`).join('')}</div>`; }
function renderFilters(config) { const target = document.querySelector('[data-panel-filters]'); if (!target) return; target.innerHTML = `<div class="form-card"><h3>Filtros e ações em lote</h3><div class="grid grid-4"><input data-panel-filter-q placeholder="Buscar por nome, obra, cidade, email..."/><select data-panel-filter-status><option value="all">Todos os status</option>${config.statusOptions.map((status) => `<option value="${status}">${status}</option>`).join('')}</select><select data-panel-bulk-status><option value="">Novo status em lote</option>${config.statusOptions.map((status) => `<option value="${status}">${status}</option>`).join('')}</select><button class="button secondary" type="button" data-panel-bulk-apply>Aplicar aos selecionados</button></div></div>`; }
function renderTable(config, items) { const target = document.querySelector('[data-panel-table]'); if (!target) return; const visible = items || filteredItems(); renderMetrics(visible); if (!visible.length) { target.innerHTML = `<div class="card"><h3>${config.empty}</h3><p>Nenhum registro corresponde aos filtros atuais.</p></div>`; return; } const header = '<th><input type="checkbox" data-select-all /></th>' + config.fields.map(([, label]) => `<th>${label}</th>`).join('') + '<th>Ação</th>'; const rows = visible.map((item) => { const cells = config.fields.map(([field]) => `<td>${formatValue(getPath(item, field), field)}</td>`).join(''); const currentStatus = itemStatus(item); const options = config.statusOptions.map((status) => `<option value="${status}" ${status === currentStatus ? 'selected' : ''}>${status}</option>`).join(''); return `<tr><td><input type="checkbox" data-row-select="${item.id}" /></td>${cells}<td><select data-status-select="${item.id}">${options}</select><button class="tag" type="button" data-save-status="${item.id}">Salvar</button><button class="tag" type="button" data-open-detail="${item.id}">Detalhes</button></td></tr>`; }).join(''); target.innerHTML = `<div class="card"><h3>${config.title}</h3><div style="overflow:auto"><table class="compare-table"><thead><tr>${header}</tr></thead><tbody>${rows}</tbody></table></div></div>`; }
async function loadPanel() { const config = currentConfig(); if (!config) return; if (!panelToken()) { panelStatus('Informe o token administrativo para carregar dados.', true); return; } try { panelStatus('Carregando dados...'); const data = await panelRequest(config.endpoint); const items = data[config.listKey] || []; window.__ARANDU_PANEL_ITEMS__ = items; renderFilters(config); renderTable(config, items); panelStatus(data.mode === 'demo' ? 'Modo demonstração: Supabase não configurado.' : 'Dados carregados.'); } catch (error) { panelStatus(error.message, true); } }
async function saveStatus(id, forcedStatus = null) { const panel = document.body.dataset.operationalPanel; const config = currentConfig(); const select = document.querySelector(`[data-status-select="${id}"]`); if (!config) return; const field = panel === 'certificados' ? 'verification_status' : 'status'; const status = forcedStatus || select?.value; if (!status) return; try { panelStatus('Atualizando status...'); await panelRequest(config.endpoint, { method: 'PATCH', body: JSON.stringify({ id, [field]: status }) }); panelStatus('Status atualizado.'); } catch (error) { panelStatus(error.message, true); } }
async function bulkApply() { const ids = Array.from(document.querySelectorAll('[data-row-select]:checked')).map((input) => input.dataset.rowSelect); const status = document.querySelector('[data-panel-bulk-status]')?.value; if (!ids.length || !status) { panelStatus('Selecione registros e um novo status.', true); return; } for (const id of ids) await saveStatus(id, status); await loadPanel(); }
function exportCsv() { const items = filteredItems(); if (!items.length) return; const keys = Array.from(items.reduce((set, item) => { Object.keys(item).forEach((key) => set.add(key)); return set; }, new Set())); const csv = [keys.join(','), ...items.map((item) => keys.map((key) => `"${String(item[key] ?? '').replace(/"/g, '""')}"`).join(','))].join('\n'); const blob = new Blob([csv], { type: 'text/csv;charset=utf-8' }); const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = `arandu-${document.body.dataset.operationalPanel}.csv`; link.click(); URL.revokeObjectURL(url); }
document.addEventListener('click', async (event) => { const save = event.target.closest('[data-save-status]'); if (save) { await saveStatus(save.dataset.saveStatus); await loadPanel(); } if (event.target.closest('[data-panel-refresh]')) loadPanel(); if (event.target.closest('[data-panel-export]')) exportCsv(); if (event.target.closest('[data-panel-bulk-apply]')) bulkApply(); if (event.target.closest('[data-select-all]')) document.querySelectorAll('[data-row-select]').forEach((input) => { input.checked = event.target.checked; }); });
document.addEventListener('input', (event) => { if (event.target.matches('[data-admin-token]')) setPanelToken(event.target.value); if (event.target.matches('[data-panel-filter-q]')) renderTable(currentConfig()); });
document.addEventListener('change', (event) => { if (event.target.matches('[data-panel-filter-status]')) renderTable(currentConfig()); });
document.addEventListener('DOMContentLoaded', () => { document.querySelectorAll('[data-admin-token]').forEach((input) => { input.value = panelToken(); }); loadPanel(); });
