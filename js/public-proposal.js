(function(){
  const root=document.querySelector('[data-public-proposal]'); if(!root)return;
  const token=new URLSearchParams(location.search).get('token');
  function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
  function itemCard(item,i){return '<article class="admin-proposal-item"><div class="admin-proposal-thumb '+esc(item.thumb||'')+'"></div><div><p class="eyebrow">Obra '+(i+1)+'</p><h2>'+esc(item.title)+'</h2><p>'+esc(item.artist)+'</p><p>'+esc([item.technique,item.dimensions,item.priceLabel].filter(Boolean).join(' · '))+'</p><p>'+esc(item.note||item.context||'Seleção curatorial Arandu.')+'</p><a href="'+esc(item.url||'comprar-arte.html')+'">Ver obra</a></div></article>';}
  if(!token){root.innerHTML='<h1>Proposta não encontrada.</h1><p>Token público ausente.</p>';return;}
  fetch('/api/selections?token='+encodeURIComponent(token),{cache:'no-store'}).then(r=>r.json()).then(json=>{const s=json.selection;if(!s)throw new Error('Seleção não encontrada.');const items=s.items||[];root.innerHTML='<p class="eyebrow">Proposta curatorial Arandu</p><h1>Seleção de arte brasileira contemporânea.</h1><p><strong>Cliente:</strong> '+esc(s.name||'Cliente em análise')+'<br><strong>Ambiente:</strong> '+esc(s.briefing?.space||s.briefing?.environment||'Ambiente a definir')+'</p><div class="admin-proposal-items">'+items.map(itemCard).join('')+'</div><section class="certificate-preview"><p class="eyebrow">Próximo passo</p><p>A proposta não confirma reserva. A Arandu verifica disponibilidade, certificado, envio e condição antes de qualquer fechamento.</p><div class="page-actions"><a class="cta" href="contato.html">Falar com a curadoria</a><button class="cta secondary" onclick="window.print()">Salvar PDF</button></div></section>';}).catch(e=>{root.innerHTML='<h1>Proposta indisponível.</h1><p>'+esc(e.message)+'</p>';});
})();
